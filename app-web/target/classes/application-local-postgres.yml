spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agenticcare
    driver-class-name: org.postgresql.Driver
    username: agenticcare
    password: agenticcare
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: false
