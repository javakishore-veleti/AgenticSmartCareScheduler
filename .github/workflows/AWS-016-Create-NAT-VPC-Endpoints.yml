name: "AWS-016: Create NAT & VPC Endpoints"
on:
  workflow_dispatch:
  workflow_call:

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.AGENTIC_SCS_CLAIMS_PROC_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AGENTIC_SCS_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AGENTIC_SCS_AWS_REGION || 'us-east-1' }}

      - name: Clean up failed stack if exists
        run: |
          STATUS=$(aws cloudformation describe-stacks --stack-name smartcare-nat-vpc-endpoints \
            --query 'Stacks[0].StackStatus' --output text 2>/dev/null || echo "DOES_NOT_EXIST")
          if [[ "$STATUS" == *FAILED* ]] || [[ "$STATUS" == *ROLLBACK_COMPLETE* ]]; then
            echo "Stack in $STATUS state — deleting before re-deploy..."
            aws cloudformation delete-stack --stack-name smartcare-nat-vpc-endpoints
            aws cloudformation wait stack-delete-complete --stack-name smartcare-nat-vpc-endpoints
            echo "Deleted."
          else
            echo "Stack status: $STATUS — proceeding with deploy."
          fi

      - name: Deploy NAT & VPC Endpoints
        run: |
          aws cloudformation deploy \
            --stack-name "smartcare-nat-vpc-endpoints" \
            --template-file DevOps/AWS/infra/cfn-nat-vpc-endpoints.yaml \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset \
            --tags Project=SmartCareScheduler Component=NAT-VPC-Endpoints
